name: Run Intelligence Engine

on:
  workflow_dispatch: # 수동 실행 버튼
  schedule:
    - cron: '*/5 * * * *' # 5분마다 실행

jobs:
  run-hybrid:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Public 리포에 쓸 권한
    
    steps:
      - name: Checkout Public Repo
        uses: actions/checkout@v4
        with:
          path: public_web

      - name: Checkout Private Brain
        uses: actions/checkout@v4
        with:
          repository: jenadoe/project1-core  # 👈 본인 아이디 확인!
          token: ${{ secrets.PAT_FOR_PRIVATE_CODE }}
          path: private_core

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
          cache: 'pip'

      - name: Run Engine
        run: |
          cd private_core
          
          # 📦 라이브러리 설치 (이게 빠져서 에러 난 것!)
          pip install -r requirements.txt
          
          # 엔진 실행
          python main.py
          
          # 데이터 이동 (Private -> Public)
          # 만약 private_core/data 폴더가 없으면 에러 날 수 있으니 체크
          if [ -f "data/market_signals.json" ]; then
            cp data/market_signals.json ../public_web/data/
          else
            echo "⚠️ Warning: No signal file generated."
          fi

      - name: Commit & Push
        run: |
          cd public_web
          git config --global user.name 'PolyBot'
          git config --global user.email 'bot@noreply.github.com'
          git add data/
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update Signals" && git push)
